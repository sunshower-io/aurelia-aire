template
    aire-application
        aire-header(sticky dark)
            navbar-section(left)
                navbar-item
                    aire-button(icon="fa fa-bars" square click.delegate="toggleOffCanvas()" primary)
            navbar-section(center)
                navbar-item(icon="dist/icons/logo.svg" large logo)
                navbar-item(logo) Aurelia + UIkit
        aire-body(view.ref="body")
            aire-offcanvas(view-model.ref="offcanvas" host.bind="body")
                aire-search(navbar icon offcanvas-header)
                aire-nav(router.bind="router")
            router-view