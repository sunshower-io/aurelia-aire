template
    header.aire-header
        slot(name="header")
    article.aire-body
        slot(name="body")
    aside.aire-aside
        slot(name="aside")
    article.aire-footer
        slot(name="footer")
