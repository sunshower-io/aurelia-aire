template(ref="container")
    .uk-form-label(if.bind="label") ${label}
    .uk-form-controls
        select.uk-select(ref="input" disabled="${disabled}")
            option(repeat.for="option of options" value="option.value") ${option.label}