template
    template(if.bind="!ordered")
        ul.uk-list(ref="list")
            li(repeat.for="item of items") ${item}
    template(if.bind="ordered")
        ol.uk-list(ref="list")
            li(repeat.for="item of items") ${item}
